1) не оставлять пользователю инициативу в написании кода; 
2) без воды — кратко описывать, что сделал; 
3) код отдавать целыми исправленными файлами/блоками, без diff; 
4) строить масштабируемую структуру; 
5) код писать максимально компактно; 
6) максимально разделять ответственности между файлами (никаких общих файлов) и на фронте и на бэке;
7) API-ручки разделять: отдельные каталоги под модули, каждая ручка в отдельном файле; 
8) не собирать легаси, меньше фолбэков; 
9) отслеживать и удалять неиспользуемый код и предупреждать при удалении; 
10) комментарии в начале файла — кратко и ясно, что внутри; (эта хуйня не прижилась)
11) приоритет проекта — скорость исполнения задач и расчётов.